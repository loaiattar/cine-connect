const ProfileCard = ({
  name,
  initials,
  memberSince,
  bio,
  stats,
  isLoading,
}) => {
  return (
    <div className="bg-black text-white rounded-2xl p-6 w-full max-w-xl">
      <p>ProfileCard — en construction</p>
    </div>
  );
};

export default ProfileCard;
